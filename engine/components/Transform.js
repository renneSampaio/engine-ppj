class Transform extends Component {
    constructor(gameObject, x, y) {
        super(gameObject);

        this.x = x;
        this.y = y;

        this.scaleX = 1.0;
        this.scaleY = 1.0;
    }
}